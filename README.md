# Textko PHP SDK example
A sample code for Textko PHP SDK.

## How to run?
* Run: `composer install`
* Edit `send.php` and change `YourAccessToken` with your [own token](https://textko.com/account)
* Run: `php send.php`
